% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mood_cluster.R
\name{mood_cluster}
\alias{mood_cluster}
\title{Cluster Songs into Mood Groups Using K-Means}
\usage{
mood_cluster(
  songs,
  k = 6,
  nstart = 25,
  iter.max = 200,
  seed = 2026,
  label_clusters = TRUE
)
}
\arguments{
\item{songs}{A data.frame with the required feature columns.}

\item{k}{Number of clusters (2 to 8). Default 6.}

\item{nstart}{Number of random starts. Default 25.}

\item{iter.max}{Maximum iterations. Default 200.}

\item{seed}{Random seed. Default 2026.}

\item{label_clusters}{Assign mood labels if TRUE. Default TRUE.}
}
\value{
A list of class "mood_result".
}
\description{
Applies k-means clustering to the six audio features and groups songs
into k clusters. Each cluster is optionally assigned a mood label based
on the closest predefined mood profile.
}
\examples{
## Example 1: Basic usage
songs <- load_songs()
result <- mood_cluster(songs)
print(result)

## Example 2: Specify number of clusters
result_k4 <- mood_cluster(songs, k = 4)
print(result_k4)

## Example 3: Access clustered data
head(result$songs)

## Example 4: View cluster centers
result$centers

## Example 5: Disable mood labeling
result_raw <- mood_cluster(songs, label_clusters = FALSE)
print(result_raw)

## Example 6: Reproducibility with seed
result1 <- mood_cluster(songs, seed = 123)
result2 <- mood_cluster(songs, seed = 123)
identical(result1$cluster_sizes, result2$cluster_sizes)
}
