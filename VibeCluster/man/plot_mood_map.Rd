% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_mood_map.R
\name{plot_mood_map}
\alias{plot_mood_map}
\title{Plot Mood Map (PCA Scatter)}
\usage{
plot_mood_map(
  result,
  show_songs = TRUE,
  show_labels = TRUE,
  show_hulls = TRUE,
  show_legend = TRUE,
  label_songs = FALSE,
  title = "Mood Map",
  alpha_songs = 0.5
)
}
\arguments{
\item{result}{A `mood_result` object returned by `mood_cluster()`.}

\item{show_songs}{Logical. If TRUE, plots individual song points.}

\item{show_labels}{Logical. If TRUE, shows labels for cluster centres.}

\item{show_hulls}{Logical. If TRUE, draws boundaries around clusters.}

\item{show_legend}{Logical. If TRUE, displays legend.}

\item{label_songs}{Logical. If TRUE, prints song titles (can be cluttered).}

\item{title}{Character. Title of the plot.}

\item{alpha_songs}{Numeric. Transparency level for song points (0 to 1).}
}
\value{
Invisibly returns a data.frame with:
\describe{
  \item{PC1}{First principal component}
  \item{PC2}{Second principal component}
  \item{mood}{Assigned mood cluster}
  \item{title}{Song title (if available)}
}
}
\description{
Creates a 2D scatter plot of songs using Principal Component Analysis (PCA).
Each song is shown as a point, coloured by its assigned mood cluster.
Cluster centres (centroids) are highlighted and optionally labelled.
}
\details{
The function uses PCA to reduce the 6 audio features into 2 dimensions:
PC1 and PC2. This allows visualisation of high-dimensional data.

The plot includes:
- Individual songs (points)
- Cluster centroids (larger markers)
- Optional convex hulls around clusters
- Optional labels and legend
}
\examples{
# Load data
songs <- load_songs()

# Cluster songs
result <- mood_cluster(songs, k = 6)

# Plot mood map
plot_mood_map(result)

# Without hulls
plot_mood_map(result, show_hulls = FALSE)

# Show song labels (only for small datasets)
plot_mood_map(result, label_songs = TRUE)

}
